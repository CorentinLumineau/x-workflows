# Mode: ask (Redirected)

> **This mode has been promoted to a standalone skill.**

## Use `/x-ask` instead

The quick Q&A functionality is now available as the standalone `/x-ask` skill, providing:

- Zero-friction Q&A with intelligent routing
- Codebase, library, web, and knowledge sources
- Context7 MCP integration
- x-explorer agent delegation

**Invocation**: `/x-ask {question}`

See: `@skills/x-ask/` for the full skill definition.

## Migration Note

| Before (v6.x) | After (v7.x) |
|----------------|---------------|
| `/x-research ask` | `/x-ask` |
| `/x-research` (for quick Q&A) | `/x-ask` |
| `/x-research` (for deep research) | `/x-research` (unchanged) |
